{
  "name": "Sellix",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "esbuild --watch --serve 'src/**/*.js' --servedir=src",
    "build": "esbuild './src/home/*' './src/home-animations/*' --bundle --sourcemap --minify --outdir=dist --watch"
  },
  "keywords": [],
  "author": "Maksym Chechel",
  "license": "ISC",
  "dependencies": {
    "gsap": "npm:@gsap/shockingly@^3.12.5",
    "split-type": "^0.3.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
